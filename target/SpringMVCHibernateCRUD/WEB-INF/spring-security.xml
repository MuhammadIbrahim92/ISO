<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/Reporting**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/Dashboard**" access="isAuthenticated()" />
	     <intercept-url pattern="/employees**" access="isAuthenticated()" />
		<intercept-url pattern="/ETL**" access="isAuthenticated()" />
		<!-- access denied page -->
		<access-denied-handler error-page="/403" />
		<form-login 
		    login-page="/login" 
		    default-target-url="/Dashboard" 
			authentication-failure-url="/login?error" 
			username-parameter="username"
			password-parameter="password" />
		<logout logout-success-url="/login?logout"  />
		<!-- enable csrf protection -->
		<csrf/>
	</http>
	
	
	<!-- Select users and user_roles from database -->
	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query=
					"select user_name,user_pass, user_active,display_name from isousers where user_name=?"
				authorities-by-username-query=
					"select user_name, role_name from user_role where user_name =?  " />
		</authentication-provider>
	</authentication-manager>

</beans:beans>